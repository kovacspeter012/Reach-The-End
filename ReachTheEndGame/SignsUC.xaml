<UserControl x:Class="ReachTheEndGame.SignsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReachTheEndGame"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300"
             x:Name="selfSignsUC">
    <Grid DataContext="{Binding ElementName=selfSignsUC}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Margin="2" Source="{Binding Source}"/>
        <TextBlock Grid.Column="1" Margin="2" VerticalAlignment="Center" TextWrapping="WrapWithOverflow" FontSize="12" Text="{Binding Text}"></TextBlock>
    </Grid>
</UserControl>
